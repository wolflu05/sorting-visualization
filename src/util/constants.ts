export const defaultSettings: {
  theme: "dark" | "light";
  size: number;
  range: [number, number];
  speed: number;
  algorithm: string;
} = {
  theme: "dark",
  size: 30,
  range: [7, 100],
  speed: 1,
  algorithm: "selectionSort",
};
